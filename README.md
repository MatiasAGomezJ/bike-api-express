1.

```bash
$ npm install
```

2.

```bash
$ npm run serverstart
```

URLs:

-   Front, todas son accesibles desde el indice:
    -   `/`
    -   `/bike`
        -   `/update`
        -   `/create`
        -   `/delete`
    -   `/store`
        -   `/update`
        -   `/create`
        -   `/delete`
    -   `/stock`
        -   `/update`
        -   `/create`
        -   `/delete`
